{
  "compilerOptions": {
    "target": "ES2021",                                  /* Set the JavaScript language version for emitted JavaScript and include compatible library declarations. */
    "module": "ES2022",                                /* Specify what module code is generated. */                                 /* Specify the root folder within your source files. */
    "moduleResolution": "node",                       /* Specify how TypeScript looks up a file from a given module specifier. */                            /* Specify multiple folders that act like './node_modules/@types'. */
    "types": ["bun-types"],                                      /* Specify type package names to be included without being referenced in a source file. */            /* Allow 'import x from y' when a module doesn't have a default export. */
    "esModuleInterop": true,                             /* Emit additional JavaScript to ease support for importing CommonJS modules. This enables 'allowSyntheticDefaultImports' for type compatibility. */                        /* Disable resolving symlinks to their realpath. This correlates to the same flag in node. */
    "forceConsistentCasingInFileNames": true,            /* Ensure that casing is correct in imports. */
    "strict": true,                                      /* Enable all strict type-checking options. */
    "jsx": "react",
    "jsxFactory": "Html.createElement",
    "jsxFragmentFactory": "Html.Fragment"                               /* Skip type checking all .d.ts files. */
  },
  "include": [
    "src/**/*.js",          // Include all JavaScript files in src and subdirectories
    "src/**/*.jsx",         // Include all JSX files in src and subdirectories
    "public/**/*.jsx",      // Include all JSX files in public and subdirectories
    "lib/**/*.js"
  ],
}
